#include "main.h"

/**
 * -abs -computes the absolute values of an integer
 *
 *  @n: the int to check
 *
 *  Return: the absolute values of int
 *
 */
int (_abs(int n))
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
